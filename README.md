# EasyKey

EasyKey 是用于自定义实体快捷键单击、长按和双击动作的 KernelSU 模块。

## 目录

- `src/`：EasyKey 后端源码
- `scripts/`：构建与测试脚本
- `module/`：可直接打包的模块文件
- `dist/`：本地构建产物

## 命令

```powershell
.\scripts\build.ps1
.\scripts\test.ps1
.\package.ps1
```
